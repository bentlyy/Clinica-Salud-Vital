tunnel: ${TUNNEL_ID}
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: clinic.tu-dominio.com
    service: http://frontend:5173
  - hostname: clinic-api.tu-dominio.com
    service: http://api:3000
  - service: http_status:404
